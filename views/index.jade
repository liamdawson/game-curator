doctype html
html(lang="en")
  head
    title Page!
  body
    h1 Game Curator
    form
      each game in games
        p
          label
            input(type="checkbox")
            =game.name
          if game.expansions
            each expansion in game.expansions
              label
                input(type="checkbox")
                = game.name + "—" + expansion.name 
